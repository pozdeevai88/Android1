<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Android1Lesson1" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="NumericButtonStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:backgroundTint">@color/_buttonNumericColor</item>
        <item name="android:textSize">@dimen/_buttonTextSize</item>
        <item name="android:textColor">@color/_buttonTextColor</item>
    </style>

    <style name="ActionButtonStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:backgroundTint">@color/_buttonActionColor</item>
        <item name="android:textSize">@dimen/_buttonTextSize</item>
        <item name="android:textColor">@color/_buttonTextColor</item>
    </style>

</resources>